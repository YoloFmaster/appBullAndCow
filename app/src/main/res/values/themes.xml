<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BullAndCow" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.BullAndCow" parent="Base.Theme.BullAndCow" />

    <style name="editTxt">
        <item name="android:layout_width">56dp</item>
        <item name="android:layout_height">48dp</item>
        <item name="android:inputType">number</item>
        <item name="android:textAlignment">center</item>
        <item name="android:textSize">25sp</item>
        <item name="android:textColorHint">#3F51B5</item>
        <item name="android:maxLength">1</item>
        <item name="android:importantForAccessibility">no</item>
    </style>

    <style name="img">
        <item name="android:layout_width">56dp</item>
        <item name="android:layout_height">48dp</item>
        <item name="android:visibility">invisible</item>
        <item name="android:contentDescription">@string/inspectCow</item>

    </style>
</resources>